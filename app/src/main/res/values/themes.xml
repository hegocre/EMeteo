<resources>
    <!-- Base application theme. -->
    <style name="Theme.EMeteo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorPrimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowBackground">@color/lightBackground</item>
        <!-- Customize your theme here. -->
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
        <!-- Material drawer. -->
        <item name="materialDrawerStyle">@style/Widget.MaterialDrawerStyleCustom</item>
        <item name="materialDrawerHeaderStyle">@style/Widget.MaterialDrawerHeaderStyle</item>
    </style>

    <style name="Theme.Toolbar" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.AppBar" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="WeatherCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="cardBackgroundColor">@color/colorBackgroundCard</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="SplashTheme" parent="Theme.EMeteo">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="Widget.MaterialDrawerStyleCustom" parent="Widget.MaterialDrawerStyle">
        <item name="materialDrawerInsetForeground">#4000</item>
        <!-- MaterialDrawer specific values -->
        <item name="materialDrawerBackground">@color/colorBackground</item>
        <item name="materialDrawerPrimaryText">?android:textColorPrimary</item>
        <item name="materialDrawerPrimaryIcon">?android:textColorPrimary</item>
        <item name="materialDrawerSecondaryText">?android:textColorSecondary</item>
        <item name="materialDrawerSecondaryIcon">?android:textColorSecondary</item>
        <item name="materialDrawerDividerColor">?android:textColorHint</item>
        <item name="materialDrawerSelectedBackgroundColor">@color/colorSecondary</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="colorAccent">@color/colorSecondary</item>
    </style>

    <style name="NegativeButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorSecondary</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorSecondary</item>
    </style>

    <style name="AboutButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginStart">20dp</item>
        <item name="android:layout_marginEnd">20dp</item>
        <item name="backgroundTint">@color/buttonBackgroundColor</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
        <item name="cornerRadius">300dp</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>